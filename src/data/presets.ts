import { AIRCOND_DATA } from './presets/aircond';
import { LORI_DATA } from './presets/lori';
import { CLEANING_DATA } from './presets/cleaning';

export const ALL_PRESETS: Record<string, any> = {
  aircond: AIRCOND_DATA,
  lori: LORI_DATA,
  cleaning: CLEANING_DATA,
};
